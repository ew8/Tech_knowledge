### new vs init
__new__  __init__

new是在创建一个实例 init则是在初始化实例的内容

比如单例模式时候
就可以在new的时候判断对象是否已经存在并返回新的或者已有的

正常情况下当你a=b()的时候 b的new 和init一定会执行，除非：
new方法返回的不是当前实例 比如
class test:
    def __new__(cls, *args, **kwargs):
        return new fun()

这时候init就不会执行
