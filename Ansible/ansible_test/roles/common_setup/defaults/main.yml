---
ntp_servers:
  - ntp.aliyun.com
  - ntp.tencent.com

chrony_package_name: chrony
chrony_service_name: "{{ 'chronyd' if ansible_facts.os_family == 'RedHat' else 'chrony' }}"
chrony_config_path: "{{ '/etc/chrony.conf' if ansible_facts.os_family == 'RedHat' else '/etc/chrony/chrony.conf' }}"
