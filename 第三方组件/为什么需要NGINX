一个Django项目，开发时候我们直接runserver并且访问即可。
但是很多生产部署时候，大多数在前面会放一个NGINX,而不是直接暴露django。
有这么几个原因：
  1.如果后端有多个服务，nginx就是迎宾，你告诉他你需要的路由，他会寻找并指向内网的服务，对于用户只需要一个域名，不需要具体知道哪个服务。
  2.所有请求都从这个迎宾进去，所以限流，安全限制，白名单等全局设置可以直接在这一层限制，而无需每个服务都配置一遍。
  3.性能问题，用django/gunicorn举例，一般我们会启动特定数量的worker来处理请求，一般取决于服务器核心数量，假设我们开了12个worker：
  一次API请求：客户端发起请求->握手->分析请求->生成resposne->传输resp->挥手请求->关闭连接。 这途中传输resp实际上会非常慢，尤其对CPU来说，毫秒级别的计算生成结果，传输可能好几秒，还取决于客户的网络，由于一般都是TCP长连接，这个过程会非常耗时，直到可靠传输结束。
  这时候nginx的用处就来了， 所有连接发给NGINX, NGINX转发到后端，获取结果后关闭连接，一般NGINX和django服务都在一个局域网，传输速度回很快，线程很快就释放并且投入到下一个请求。
  而NGINX会慢慢处理每一个请求的resp回送，NGINX是事件驱动型，翻译过来就是我并不是一个线程长连接发resp，而是几个线程盯很多的连接，比如A连接发了100kb数据之后还没有回复ack，那么线程会继续轮询下一个连接，即哪个连接ready我就发送，每一个连接都有一个socket缓冲区。
  判断依据是我将resp填充至缓冲区，如果网速够快缓冲区很快清理了那么我就继续发送，如果缓冲区满了，那么我就继续查询下一个连接，从而做到用少量线程盯超多连接。
  同理，所有前端静态资源都用nginx回复而不是django，因为静态资源相对API resp大很多，kb甚至MB的图片或者视频，这种如果让django回复也会出现上面聊到的慢的问题，而nginx就专注于慢慢发送，让django专注于API的计算。
