# 僵尸进程

## 直接描述
僵尸进程, 子进程没了, 父进程不回收  
孤儿进程, 子进程还在, 父进程没了. 变成了孤儿. 这个时候父进程变成systemd, 也就是pid1号进程.
当一个僵尸进程的父进程没了, 僵尸瞬间变成孤儿, 然后, pid1 瞬间将孤儿干死, 没有进程泄露.

docker 容器不一样, docker 容器内的1号进程 pid1没有systemd的能力. 也就是说 如果你开启一个容器, 不停地让pid1 开启短时子进程会导致宿主机进程泄露. 结论：
1. 容器内不要开启大于1个进程
2. 如果大于1个, pid1 最好是https://github.com/krallin/tini

## 详情

linux启动后，PID1固定是init进程，即由init这个万物的父亲调用所有的其他进程，init也拥有对所有进程的监控能力，所以他有能力监控所有进程是否处于僵尸进程的状态。当每一个父进程退出的时候，内核会将其子进程（一般是僵尸进程）过继到init下面，init二次确认后，会逐步执行清理操作，最终关闭这个进程。

前提是这个僵尸进程已经失去了原有的父进程，如果父进程一直或者，但是由于某些原因死锁或者无法进行销毁，那么INIT不会进行干预，这时候只能认为干预，比如kill -9 PID.

Docker由于设计原因，其PID1是你entrypoint或者CMD设置的命令，这就导致了容器内实际上无法处理僵尸进程的问题，最终结果是PID耗尽，导致无法执行任何操作。于是有了上面2个建议：
1. 容器内不要开启大于1个进程
2. 如果大于1个, pid1 最好是https://github.com/krallin/tini
docker 1.3内置了tini 但是需要手动添加参数docker run --init
