大模型优势：
1. 自然语言理解与生成极强	能读懂复杂、模糊、不完整的指令，并生成流畅、连贯、风格可控的文本。
2. 多任务通用能力	不需要为每个任务单独训练模型。同一个模型可以写诗、编程、翻译、总结、分类、角色扮演……
3. 少样本 / 零样本学习	只在 Prompt 里给 1~2 个例子（few-shot）甚至不给例子（zero-shot），就能做新任务。
4. 海量知识压缩	预训练阶段记住了大量公开知识（常识、科学、文化、代码等），相当于一个通才图书馆。
5. 长上下文理解	现代大模型可一次处理几十万甚至百万 token（如 GPT-4 Turbo、Claude-3、Gemini 1.5）。能读完整本书、整份财报。
6. 可被“引导”而非“编程”	这就是你理解的 Prompt 工程的基础——不需要写规则，用自然语言就能指挥它。

劣势：
1. 幻觉（Hallucination）	它会自信地编造不存在的事实、引用、链接、法律条文等。这是最致命的问题之一。
2. 知识截止日期	训练时用的数据只到某个时间点（例如 2023 年 10 月）。之后的新事件、新发现一概不知。
3. 无法真正“推理”	看似在推理，实际上是模式匹配。多步逻辑、因果链、数学应用题很容易翻车（除非借助 C​oT 或外部工具）。
4. 偏见与有害内容	训练数据里存在的性别、种族、文化等偏见会被模型学去，而且可能生成不当内容（需对齐和过滤）。
5. 无真实记忆	每一次对话是“无状态的”（除非开发者主动把历史再塞回去）。模型不记得你昨天说过的话，除非你用工程手段保存。
6. 成本高、延迟高	大模型需要 GPU 或 TPU 集群推理，比传统数据库查询慢得多、贵得多。
7. 不可解释	你不知道它为什么给出某句话。它不像决策树或逻辑规则那样可追溯。
8. 对 Prompt 极其敏感	同样的问题换一种措辞，答案可能完全不同，甚至从正确变错误。这就是需要 Prompt 工程的原因。
9. 缺乏工具使用原生能力（未扩展时）	纯粹的大模型不会计算、不会查数据库、不会调用 API。但 Agent / Function Calling 可以解决。
10. 输出概率性	温度 > 0 时，同一个 Prompt 每次回答可能不一样（对某些场景是优势，对需要确定性的场景是劣势）


  prompt工程更多是一种引导，用更多的细节来引导ai做更确切的回答
  langchain就类似一个框架，他设计好了通用接口，你的事情就是将那些封装好的接口调用来完成你的RAG模型

指标：
召回率 (Recall)实际发生的错误中，系统正确捕获了多少
精确率 (Precision)系统报出的"错误"中，真正是错误的比例
误报率 (FPR)正常日志被误判为错误的比例
F1-Score	召回率和精确率的调和平均	2 * P * R / (P + R)
假设生产环境昨天实际发生了 100 个错误，系统报了 90 个，其中 85 个是真错误、5 个是误报 —— 那召回率 85%，精确率 94.4%

规则命中率	多少比例的错误被规则引擎直接匹配（不走 LLM）—— 越高说明知识库越成熟，成本越低
平均分析耗时	规则引擎 ≈ 毫秒级，LLM ≈ 秒级~数十秒，记录分位值（p50/p99）









这套机制不是某个论文里的现成公式，而是**经典信息检索（IR）**里几种常见思路的组合，针对你们这种「小知识库 + 规则 Agent」做了简化。

整体架构：两阶段检索
用户问题
第一阶段 retrieve / search_kb
粗召回 多条候选
第二阶段 filter_relevant_projects
只展示高关联结果
对应 IR 里很常见的模式：

召回（Recall）：先尽量不漏 —— retrieve() / search_kb
精排/过滤（Precision）：再尽量不误 —— filter_relevant_projects()
你们遇到的问题（日志 + 防火墙一起出现）本质是：召回太宽、精排不够。所以加的是第二阶段，不是换掉第一阶段。

1. 词重叠打分 —— 向量空间模型（VSM）的极简版
score_doc_relevance() 核心是：把 query 和文档都拆成词，数有多少词重合。

这来自经典 Vector Space Model（Salton, 1970s）和 TF-IDF 家族的思想：

文档和查询都可以看成「词袋」
重合越多，越可能相关
你们现在的实现比完整 TF-IDF 更简单：

完整 TF-IDF	你们的实现
词频 TF
有重叠就算 1 次（二元 0/1）
逆文档频率 IDF
没有（库很小，先不加）
余弦相似度
没有，直接加权求和
对小知识库（几十个 doc）这样够用，也更好调试。

2. 字段加权 —— 搜索引擎的 field boost

kb.py
Lines 190-196
fields = [
    (doc.get("title") or "", 4.0),
    (" ".join(doc.get("tags") or []), 3.0),
    (doc.get("summary") or "", 2.0),
    (doc.get("body") or "", 1.0),
    (doc.get("id") or "", 2.0),
]
原理来自 Lucene / Elasticsearch 一类引擎的 field boosting：

标题、标签 = 作者明确写的「这篇讲什么」→ 权重大
正文 = 细节多、泛词多 → 权重小
你们那个 bug 就是典型反例：

问：「AI 日志分析…」
防火墙案例正文里有「路径流量分析」
若正文和标题权重一样，「分析」就会把两篇拉到一起
标题加权后，「AI 日志」会明显高于「只靠正文里的分析」
4 / 3 / 2 / 1 是经验值，不是理论最优；思路是「越靠近文档主题的字段，越可信」。

3. 弱词 / 停用词 —— Stopword 思想

kb.py
Lines 124-160
# 泛化词：单独命中不足以说明和用户问题相关
_WEAK_TOKENS = frozenset([ "分析", "什么", "是", ... ])
对应 IR 里的 stopwords（停用词）：

「的、是、什么、分析、项目」等在大量文档里都会出现
区分度低，不能单独证明相关
所以加了 strong（强关联词命中数）：

只有弱词命中（如单独「分析」）→ strong = 0 → 直接淘汰
有「日志」「防火墙」「微服务」等 → strong ≥ 1 → 才考虑进结果
这是 precision 优先：宁可少展示一条，也不要展示明显无关的。

4. 相对阈值 —— 检索里的 score gap / 相对截断

kb.py
Lines 231-232
best_score = scored[0]["score"]
threshold = max(3.0, best_score * 0.45)
思路是：第一名和第二名要拉开足够差距，第二名才值得展示。

类似：

搜索引擎里的 score ratio cutoff（最高分的一半以下不要）
Learning to Rank 里常见的 top-1 vs top-2 margin
max(3.0, best × 0.45) 两个条件：

条件	作用
best × 0.45
第二名必须接近第一名，不能只是蹭到一个泛词
3.0 绝对下限
防止大家都很低分时，仍勉强展示第二条
0.45 和 3.0 同样是调参经验值，可按实际问答再调。

5. anchor_id —— 显式实体解析优先
若 get_project 已从用户话里认出案例（如「日志」→ ai-log-analysis），该案例强制保留。

这对应 NLP 里 entity linking / slot filling 的思路：

用户已经点名主题 → 规则解析结果优先于模糊检索
检索结果只作补充，不能推翻已解析的意图
和你们场景的对应关系
以「AI 日志分析是做什么的？」为例：

机制	AI 日志案例	防火墙案例
标题加权
标题含「日志」「AI」→ 高分
标题无这些词
强词
strong ≥ 1（日志、AI）
只有「分析」→ strong = 0
相对阈值
第一名
分数远低于第一名 → 过滤
为什么不用 embedding / LLM 重排？
也可以，但当前选择是刻意的：

方案	优点	缺点
现在这套
可解释、无 API 成本、回归可测
同义词、改写能力弱
向量检索
语义相近能匹配
要模型、要维护向量库
LLM rerank
最灵活
慢、贵、结果不稳定
你们知识库小、案例少、还要和 regress_phase0.py 对齐，可解释的规则精排更合适。以后库变大或问法更杂，可以在第二阶段换成 cross-encoder 或 LLM rerank，第一阶段 retrieve 可以不动。

一句话总结
粗召回用词重叠（VSM），精排用字段加权 + 停用词 + 相对阈值（经典 IR 三板斧），再叠加规则解析的 anchor 保底。

权重数字是工程调参，不是理论推导；原理来自信息检索里「召回宽、精排严」和「高区分度特征权重大」这两条线。

如果你想，我可以把 4/3/2/1 和 0.45 抽成配置项，或者画一张和 retrieve() 第一阶段的对比表，方便以后调参。
