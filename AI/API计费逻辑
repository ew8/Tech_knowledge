**API计费逻辑**

**Input Token**: 用户输入的内容的计费  
**Output Token**: 系统输出的内容的计费。

一般来说输出会远贵于输入，以deepseek为例， 百万输入缓存大概0.1 未命中缓存1.5~3 而输出则是4.5~9.

这里提到了缓存命中，这个跟普通开发业务中的缓存意思基本类似，比如我们会有个redis在数据库前面做快速查询，有些热点信息你每次查DB慢还增加消耗，如果直接放在redis会显著提升效率。这个缓存命中意思跟这个差不多，有部分热点数据放在了缓存中，如果正好你问的问题有人问过（命中了redis），那么直接回复了，价格就很低。没命中就去数据库硬查（实际是重新分析并生成答案），就会贵一些。

**如何触发命中？**  
大模型会把用户发来的前缀内容，与缓存成逐个token做比对。只有当完全匹配的情况下，才算缓存命中。
这个匹配也会根据内容切分成不同单元。单元也会智能的自动更新，比如有人问了A+B  第二次A+C。系统会判断未命中，但是会提炼出公共部分A。 下次你问A+D那么A单元会走缓存。
之后当如有人问过A+B  你的问题是A+B+C， 实际上A+B的部分就会使用缓存。而C部分会当成缓存未命中。  

一般情况API resp里面的usage会直接告诉你是否命中，比如hit或者miss

