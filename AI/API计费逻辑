**API计费逻辑**

Input Token: 用户输入的内容的计费  
Output Token: 系统输出的内容的计费。

一般来说输出会远贵于输入，以deepseek为例， 百万输入缓存大概0.1 未命中缓存1.5~3 而输出则是4.5~9.

这里提到了缓存命中，这个跟普通开发业务中的缓存意思基本类似，比如我们会有个redis在数据库前面做快速查询，有些热点信息你每次查DB慢还增加消耗，如果直接放在redis会显著提升效率。这个缓存命中意思跟这个差不多，有部分热点数据放在了缓存中，如果正好你问的问题有人问过，那么直接回复了，价格就很低。没命中就去数据库硬查，就会贵一些。
